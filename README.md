# ZenSpace v5.0 — Медитация

Приложение для создания и сохранения собственных медитаций с выбором музыки и фонов.

## 🚀 Запуск на компьютере

### Шаг 1: Установка зависимостей

```bash
cd zenspace
npm install
```

### Шаг 2: Запуск локально

```bash
npm run dev
```

Открой браузер: http://localhost:5173

---

## 📱 Запуск на смартфоне в локальной сети

### Шаг 1: Узнай IP адрес компьютера

**На Mac/Linux:**
```bash
ipconfig getifaddr en0
# или
ifconfig
```

**На Windows:**
```bash
ipconfig
# Ищи "IPv4 Address:" (что-то вроде 192.168.x.x)
```

Выглядит примерно так: `192.168.1.100`

### Шаг 2: Запусти dev сервер

```bash
npm run dev
```

Сервер будет доступен на:
- **Компьютер:** http://localhost:5173
- **Смартфон:** http://ТВ_IP_АДРЕС:5173

Например: `http://192.168.1.100:5173`

### Шаг 3: Открой на смартфоне

1. Подключи смартфон к одной Wi-Fi сети с компьютером
2. Открой браузер (Chrome, Safari)
3. Введи адрес: `http://192.168.1.100:5173` (замени IP на свой)
4. Готово! 🎉

---

## ✨ Что работает

✅ Создание медитаций с этапами
✅ Выбор фоновой музыки (6 вариантов)
✅ Выбор фона (5 красивых картинок)
✅ Сохранение медитаций (localStorage)
✅ Редактирование сохраненных медитаций
✅ Удаление медитаций
✅ Таймер с гонгом между этапами
✅ Синтезированный звук гонга
✅ Полная адаптация под мобильные устройства

---

## 📁 Структура проекта

```
zenspace/
├── src/
│   ├── components/         # React компоненты
│   ├── hooks/              # Custom хуки
│   ├── types/              # TypeScript типы
│   ├── main.tsx            # Entry point
│   └── index.css           # Стили
├── public/
│   ├── audio/              # MP3 файлы музыки
│   └── images/             # Фоны медитации
├── package.json            # Зависимости
├── vite.config.js          # Vite конфиг
├── tailwind.config.js      # Tailwind конфиг
└── index.html              # HTML шаблон
```

---

## 🔧 Команды

```bash
# Разработка
npm run dev

# Build для продакшена
npm run build

# Проверить готовый build
npm run preview
```

---

## 💾 Сохранение данных

Все медитации сохраняются в **localStorage** браузера (локально на устройстве).

Структура сохраненной медитации:
```javascript
{
  id: "1704441600000",
  name: "Утренняя практика",
  stages: [
    { id: "1", name: "Дыхание", duration: 5 },
    { id: "2", name: "Расслабление", duration: 3 }
  ],
  selectedMusic: "nature",
  selectedBackground: "bg1",
  totalDuration: 8,
  createdAt: 1704441600000
}
```

---

## 📝 Решение проблем

### Музыка не воспроизводится
- Проверь, что файлы в `/public/audio/`
- Разреши автозапуск звука в браузере
- Попробуй другой браузер (Chrome лучше всего)

### Смартфон не видит сервер
1. Проверь IP адрес компьютера (`ipconfig` / `ifconfig`)
2. Убедись, что оба устройства в одной Wi-Fi
3. Попробуй отключить VPN
4. Проверь файрвол

### Фоны не загружаются
- Очистить кэш браузера (Ctrl+Shift+Delete)
- Перезагрузить страницу

---

## 🎨 Кастомизация

### Изменить цвета
Отредактируй переменные в `src/index.css`:
```css
--primary: 38 60% 55%;      /* Золотой цвет */
--background: 220 20% 8%;   /* Тёмный фон */
```

### Добавить новую музыку
1. Положи MP3 файл в `/public/audio/`
2. Отредактируй массив в `src/components/MusicSelector.tsx`

### Добавить новый фон
1. Положи изображение в `/public/images/`
2. Отредактируй массив в `src/components/BackgroundSelector.tsx`

---

## 🚢 Деплой на Google Play (будущее)

После тестирования на смартфоне:
1. Подключи Firebase для облачного хранилища
2. Добавь аутентификацию
3. Сделай PWA версию (прогрессивное приложение)
4. Публикуй на Play Store / App Store

---

## 📚 Технологии

- **React 18** — UI фреймворк
- **Vite** — сборщик
- **Tailwind CSS** — стили
- **TypeScript** — типизация
- **Web Audio API** — синтез звука

---

## ❓ Вопросы?

Чини код, экспериментируй, наслаждайся медитацией! 🧘‍♀️✨
